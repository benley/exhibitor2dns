from setuptools import setup

setup(
    setup_requires=['pbr'],
    pbr=True
    )

#entry_points={'console_scripts': ['exhibitor2dns = exhibitor2dns.main']},
#      name='exhibitor2dns',
#      version='0.1',
#      packages=['exhibitor2dns'])
